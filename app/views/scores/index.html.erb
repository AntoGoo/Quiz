<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Classement des Scores</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        background-color: #f8f9fa;
      }
      .score-card {
        border-radius: 10px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        transition: transform 0.2s;
      }
      .score-card:hover {
        transform: scale(1.03);
      }
    </style>
  </head>
  <body>
    <div class="container my-5">
      <div class="text-center mb-4">
        <h1 class="fw-bold">🏆 Classement des Joueurs</h1>
        <p class="text-muted">Le meilleur score en premier</p>
      </div>

      <div class="row justify-content-center">
        <% @players.each_with_index do |player, index| %>
          <div class="col-md-6">
            <div class="card score-card mb-3">
              <div class="card-header bg-<%= index == 0 ? "success" : "primary" %> text-white">
                <h3 class="mb-0">
                  <%= index == 0 ? "🥇 " : "" %><%= player.pseudo %>
                </h3>
              </div>
              <div class="card-body">
                <p class="fs-5 text-center">Score : <strong><%= player.high_score %></strong></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Inclusion du JS Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
