<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Quiz Multijoueur</title>
    <!-- Inclusion de Bootstrap 5 via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        background-color: #f8f9fa;
      }
      .card-header {
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <div class="container my-5">
      <!-- Carte d'accueil avec message de bienvenue et connexion/déconnexion -->
      <div class="card shadow mb-4">
        <div class="card-header bg-info text-white text-center">
          <h1>Bienvenue dans le Quiz Multijoueur Anto</h1>
        </div>
        <div class="card-body text-center">
          <% if session[:user_id] %>
            <p class="fs-5">Connecté en tant que <strong><%= User.find(session[:user_id]).pseudo %></strong></p>
            <%= link_to "Déconnexion", logout_path, method: :delete, class: "btn btn-danger",
                data: { confirm: "Êtes-vous sûr de vouloir vous déconnecter ?" } %>
          <% else %>
            <%= link_to "Connexion", login_path, class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>

      <!-- Carte affichant la liste des Quiz -->
      <div class="card shadow">
        <div class="card-header bg-secondary text-white">
          <h2 class="mb-0">Liste des Quiz</h2>
        </div>
        <div class="card-body">
          <div class="list-group">
            <% @quizzes.each do |quiz| %>
              <div class="list-group-item d-flex justify-content-between align-items-center">
                <%= link_to quiz.title, quiz_path(quiz), class: "text-decoration-none" %>
                <!-- Bouton pour démarrer une session de jeu -->
                <%= button_to "Jouer", game_sessions_path(quiz_id: quiz.id), method: :post, class: "btn btn-outline-success mb-0" %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <!-- Inclusion du JavaScript de Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
